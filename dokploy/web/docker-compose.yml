# SvelteKit web app (apps/web) — also the remote URL loaded by the Tauri
# desktop and mobile shells. In Dokploy set:
#   Build Context = repo root
#   Compose Path  = dokploy/web/docker-compose.yml

services:
  web:
    build:
      context: ../..
      dockerfile: dokploy/web/Dockerfile
    image: conusai/web:latest
    hostname: conusai-web
    restart: unless-stopped
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
      # SvelteKit adapter-node — required for cookie/CSRF origin checks
      ORIGIN: "https://${APP_DOMAIN}"
      PROTOCOL_HEADER: x-forwarded-proto
      HOST_HEADER: x-forwarded-host
      # Cookie scope — shared with api.${APP_DOMAIN} for SSO
      COOKIE_DOMAIN: "${COOKIE_DOMAIN}"

      # Server-side calls go to the gateway over the internal network
      CONUSAI_BACKEND_URL: "http://conusai-gateway:8080"
      UI_SESSION_KEY: "${UI_SESSION_KEY}"

      # OIDC — Zitadel (server-side flows)
      ZITADEL_ISSUER: "${ZITADEL_ISSUER}"
      ZITADEL_CLIENT_ID: "${ZITADEL_CLIENT_ID}"

      # Billing portal links
      LAGO_API_URL: "https://billing.${APP_DOMAIN}"
      LAGO_PORTAL_URL: "https://billing.${APP_DOMAIN}"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks: [dokploy-network]
    # Public routing managed by Dokploy via dokploy/domains.yaml
    # (sync with: node dokploy/scripts/sync-domains.mjs)
    # HSTS / secure headers should be configured via Dokploy → Traefik
    # File System middlewares once Domain UI supports referencing them.

  # ── One-shot domain sync ────────────────────────────────────────────────
  domain-sync:
    image: node:22-alpine
    restart: "no"
    environment:
      APP_DOMAIN: "${APP_DOMAIN:?APP_DOMAIN must be set}"
      DOKPLOY_URL: "${DOKPLOY_URL:?DOKPLOY_URL must be set in Shared Env}"
      DOKPLOY_API_KEY: "${DOKPLOY_API_KEY:?DOKPLOY_API_KEY must be set in Shared Env}"
      DOKPLOY_ENVIRONMENT_ID: "${DOKPLOY_ENVIRONMENT_ID:?DOKPLOY_ENVIRONMENT_ID must be set in Shared Env}"
      DOKPLOY_APP_NAME: web
    volumes:
      - ../scripts/sync-domains.mjs:/sync.mjs:ro
      - ../domains.yaml:/domains.yaml:ro
    command: ["node", "/sync.mjs", "--app", "web", "--trigger-redeploy"]
    networks: [dokploy-network]

networks:
  dokploy-network:
    external: true
