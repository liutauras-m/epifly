schema_version = "2.0"
name        = "storage-put"
version     = "0.1.0"
namespace   = "storage.object.put"
category    = "storage"
kind        = "native"
description = "Write a file (text or base64-encoded binary) to the tenant workspace."
tags        = ["storage", "write", "put", "object"]
accepts     = ["application/json"]
emits       = ["application/json"]
idempotent  = true
requires    = []
search_keywords = ["put", "write", "upload", "store file"]

[[tools]]
name = "put_object"
description = "Write content to a workspace path. Supply content (text) or content_base64 (binary)."

[tools.input_schema]
type = "object"
required = ["path"]

[tools.input_schema.properties.path]
type = "string"
description = "Relative workspace path to write to (e.g. uploads/2026/01/file.pdf)"

[tools.input_schema.properties.content]
type = "string"
description = "Text content to write"

[tools.input_schema.properties.content_base64]
type = "string"
description = "Base64-encoded binary content to write"

[config]
op = "put_object"
