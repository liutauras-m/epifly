import type { ComponentFixtureSet } from '../gallery.types.js';

const fixtures: ComponentFixtureSet = {
  label: 'ThinkingIndicator',
  note: 'Animated sonar ring indicator shown while the AI is generating a response. No props — self-contained.',
  cases: [
    { label: 'Default', props: {} },
  ],
};

export default fixtures;
