import type { ComponentFixtureSet } from '../gallery.types.js';

const fixtures: ComponentFixtureSet = {
  label: 'ThemeProvider',
  note: 'Theme context provider — wraps the app and supplies ThemeStore via context (Phase 5).',
  cases: [
    { label: 'system theme', props: {} },
  ],
};
export default fixtures;
