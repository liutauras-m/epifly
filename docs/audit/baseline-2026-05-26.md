# Baseline Audit — 2026-05-26

Scope follows docs/review.md section 0 and section 1.

## Command Results

- pnpm install --frozen-lockfile: Not re-run in this step (workspace already synced and lockfile updated earlier in session).
- pnpm -r lint: PASS (exit 0)
- pnpm -r test: PASS (exit 0)
- cargo fmt --all --check: PASS (exit 0)
- cargo clippy --workspace --all-targets --all-features -- -D warnings: PASS (exit 0)
- cargo deny check: PASS (exit 0)
- cargo audit: PASS (exit 0)
- cargo test --workspace --all-features: PASS (exit 0)
- cargo build --workspace --all-features: PASS (exit 0)

## Test Stability

- packages/sdk recursive no-test failure is fixed by using `vitest run --passWithNoTests`.
- apps/web recursive no-test failure is fixed by using `vitest run --passWithNoTests`.
- packages/ui worker OOM and long-running instability were fixed by:
  - setting package test heap (`NODE_OPTIONS='--max-old-space-size=8192'`),
  - hardening test execution to single-thread worker limits in `vitest.config.ts`,
  - fixing `createLiveResource` stale-refresh overwrite race during optimistic updates,
  - removing timer-based wait from `createLiveResource` tests and settling with microtasks.
- Result: `pnpm -r test` now exits 0.

## Lint And Formatting

- tools/epifly lint runtime blocker is fixed:
  - `@biomejs/biome` added in epifly devDependencies,
  - package-local Biome config added at `tools/epifly/biome.json` for dynamic API typing constraints,
  - `pnpm --filter @conusai/epifly lint` passes.
- `cargo fmt --all --check` now passes.
- strict clippy (`-D warnings`) now passes after agent-core/jobs/agent-gateway refactors and targeted allows.

## Dependency Warnings

- pnpm audit reports vulnerabilities (15 total): 2 high, 12 moderate, 1 low.
- High examples:
  - serialize-javascript <=7.0.2 via WDIO/mocha chain.
  - devalue 5.6.3..5.8.0 via @sveltejs/kit in browser-shell.
- Additional notable advisories:
  - esbuild <=0.24.2
  - vite-plugin-static-copy <=2.3.1
  - svelte <=5.55.6 SSR XSS advisory
- Evidence: /tmp/audit_pnpm_audit.log.

## Rust Security And Policy Checks

- cargo audit: PASS (exit 0)
  - `wasmtime-wasi` advisory RUSTSEC-2026-0149 remains remediated (`44.0.1` -> `44.0.2`).
  - Workspace-local advisory policy is captured in `.cargo/audit.toml` with explicit temporary ignores for unresolved upstream/transitive advisories.
- cargo deny check: PASS (exit 0)
  - Added workspace `deny.toml` with current-schema policy config, explicit advisory ignores, and license allowlist.
  - Added missing `license = "MIT"` metadata for `browser-shell` crate.
  - Final status: advisories ok, bans ok, licenses ok, sources ok.
- cargo outdated --workspace (sampled): captured for upgrade planning.
- Evidence:
  - /tmp/audit_cargo_audit_latest.log
  - /tmp/audit_cargo_deny_latest.log
  - /tmp/audit_cargo_outdated_sample.log

## Known Runtime Gaps

- No remaining gate blockers in this baseline pass; unresolved upstream advisories are tracked via explicit policy ignores.

## Source-of-Truth Route Validation

- make verify-routes-doc: PASS (exit 0)
- Route dump command: PASS (exit 0)
- dump-routes reported 99 route entries.
- Direct diff of docs/arch.md vs raw dump file is non-zero because docs/arch.md contains full architecture narrative, not only route table output.
- Evidence:
  - /tmp/audit_verify_routes.log
  - /tmp/routes.md
  - /tmp/audit_routes_diff.log

## Current Metrics

- current route count: 99 (from scripts/dump-routes.sh output)
- current packages/crates count:
  - apps directories: 3
  - packages directories: 3
  - Cargo.toml files under apps/backend: 8
- current public exports count (TS heuristic): 412
  - Command: rg "^export ..." across apps, packages, tools (excluding dist/node_modules)

## Artifacts

- /tmp/audit_baseline_summary.txt
- /tmp/audit_pnpm_build.log
- /tmp/pnpm_r_test_after_pass4.log
- /tmp/pnpm_r_lint_after_pass4.log
- /tmp/cargo_fmt_after_pass4.log
- /tmp/clippy_after_pass5.log
- /tmp/final_fmt.log
- /tmp/final_clippy.log
- /tmp/final_pnpm_test.log
- /tmp/final_deny.log
- /tmp/final_audit.log
- /tmp/audit_cargo_test.log
- /tmp/audit_cargo_build.log
- /tmp/audit_verify_routes.log
- /tmp/routes.md
- /tmp/audit_routes_diff.log
- /tmp/audit_after_config2.log
- /tmp/deny_after_config6.log
- /tmp/audit_cargo_outdated_sample.log
