# Token rename changelog

> Auto-updated by `scripts/rename-token.mjs`. Each row records a public token rename.
> Consumers that used the old name have a compatibility alias in `tokens.css` until Phase 4 close.

| Date | Old name | New name | PR |
|------|----------|----------|----|
